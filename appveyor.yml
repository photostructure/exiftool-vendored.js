environment:
  nodejs_version: "lts"

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install

test_script:
  - node platform
  - node --version
  - npm --version
  - npm test

build: off